/**
 * TODO
 */


import { apply, arrayOf, bool, constant, number, oneOf, shape, string } from  '../lib';

export { randomBool, randomChar, randomFloat, randomString } from '../lib/random';
export { Mocker } from './mocker';
export const M = { apply, arrayOf, bool, constant, number, oneOf, shape, string };
