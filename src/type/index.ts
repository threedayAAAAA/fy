export interface OptionsType {
    min?: number,
    max?: number,
    length?: number,
    chars?: string, // 指定字符串范围
    hours?: number,
    minutes?: number,
    seconde?: number,
    type?: string,
}